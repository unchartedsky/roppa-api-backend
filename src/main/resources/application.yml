server:
  port: 8080

#Actuator
management:
  server:
    port: 8000
    address: 127.0.0.1

spring:
  profiles:
    active: local
  output:
    ansi:
      enabled: detect
  jooq:
    sql-dialect: mariadb
---
